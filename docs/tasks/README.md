# Tasks Directory

This directory stores task folders in the format `task_{id}`.

- Example: `task_0`, `task_1`, `task_2`
- `id` is the next integer after the highest existing task id
- Each task folder should include `log.md` and `summary.md`
